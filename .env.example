# Public origin used for canonical metadata. No secrets are needed: the app has
# no backend, no database, and no third-party services.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Host port published by docker compose.
PORT=3000
